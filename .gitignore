# conductor — qué NO se sube.
node_modules/

# build-dist output — run node engine/build-dist.mjs to create; not committed (Jorge flips marketplace.json when ready)
dist-plugin/
*.log
.DS_Store

# scratch / experimentos / notas — NO forma parte del producto
# task/

# proyecto de prueba local (no es del plugin)
test-conductor/

# salidas de build/ejecución del motor (la fuente es engine/, el artefacto es assets/conductor.mjs)
engine/dist/
engine/.runs/
engine/test/.tmp*
engine/eval/.tmp*
engine/test/.tmp-e2e-proof.txt

# output de auto-runs del propio repo (la herramienta crea openspec/changes/<x>/ al correr sobre sí misma;
# en ESTE repo es salida de prueba, no fuente — los specs reales viven en docs/ y en los proyectos de usuario)
openspec/

# artefactos de ejecución y secretos
task/
CHANGELOG.md
**/*.key.md
**/conductor.sarif
**/conductor-junit.xml
**/provenance.ledger.jsonl
conductor.key
conductor.pub
eval/.bench/
**/.conductor/

# ── local de la máquina / Claude Code (estado de runtime, nunca compartir) ──
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
.claude/settings.local.json
.claude/*.local.json

# ── secretos / entorno local (defensa: jamás commitear credenciales) ──
.env
.env.*
*.env
byok.json

# ── OS / editor ──
Thumbs.db
ehthumbs.db
Desktop.ini
.idea/
*.swp

# ── output de cobertura de tests ──
coverage/
