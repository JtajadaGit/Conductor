# Eval case: data-migration

Tarea de migración: añadir la columna `email_verified BOOLEAN` a la tabla users.
El pipeline debe producir spec ADDED, script DDL trazado con @conductor, y tasks completadas.

Se puntúa con `rubric.json` (gate + trazabilidad a migrations/ + requisito presente).
