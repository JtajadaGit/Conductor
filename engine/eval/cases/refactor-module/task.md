# Eval case: refactor-module

Tarea de refactor: extraer la lógica de cálculo de descuento de PaymentService a una clase
DiscountCalculator separada, sin romper la interfaz pública (el contrato API debe mantenerse).

Se puntúa con `rubric.json` (gate + trazabilidad + requisito presente).
