# Eval case: quick-fix

Tarea micro: corregir un bug de validación de email. El validador actual no comprueba que exista @.
El pipeline debe producir un change con la spec MODIFIED del requisito existente y la corrección en el fichero.

Se puntúa con `rubric.json` (gate de coherencia + presencia del requisito modificado).
